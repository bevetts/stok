{
  "name": "stok — Solace OS",
  "install": "node --version && python3 --version",
  "terminals": [
    {
      "name": "static-server",
      "command": "python3 -m http.server 8000 --bind 0.0.0.0",
      "description": "Serves the static site (Solace OS at /solace/, salon.html at root) on port 8000."
    }
  ],
  "ports": [
    {
      "name": "web",
      "port": 8000
    }
  ]
}
